block variables

doctype html
html
    head
        meta(charset="utf-8")
        title #{title} 
        meta(content=`${desc}` name="description")
        meta(content="" name="keywords")
        meta(content="width=device-width, initial-scale=1" name="viewport")
        meta(content="ie=edge" http-equiv="x-ua-compatible")
        link(rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/animate.css/3.7.0/animate.min.css' media="none" onload="if(media!='all')media='all'")
        link(rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/fancybox/2.1.7/css/jquery.fancybox.min.css' media="none" onload="if(media!='all')media='all'")
        link(rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.8.2/css/all.min.css' media="none" onload="if(media!='all')media='all'")
        link(rel='shortcut icon' href='assets/images/favicon.ico' type="image/x-icon")
        link(rel='stylesheet' href='assets/styles/app.min.css')
        
    body
        block content

        .status-popup.popup#success
            .status-popup__inner
                .status-popup__message The e-mail has been sent successfully
        .status-popup.popup#error
            .status-popup__inner
                .status-popup__message An error occurred during the preparation of the despatch

        .hamburger__menu
            a.hamburger__menu-link#hamburger__menu-link(href="#")
                .hamburger__bars
        .dark-menu#dark-menu
            .dark-menu__items
                nav.nav_hamburger
                ul.nav__listt#nav__list_hamburger
                    li.nav__item_hamburger
                        a.nav__link(href="/") Home
                    li.nav__item_hamburger
                        a.nav__link(href="about.html") About us
                    li.nav__item_hamburger
                        a.nav__link(href="services.html") Services
                    li.nav__item_hamburger
                        a.nav__link(href="projects.html") Projects
                    li.nav__item_hamburger
                        a.nav__link(href="safety.html") Safety
                    li.nav__item_hamburger
                        a.nav__link(href="careers.html") Careers
                    li.nav__item_hamburger 
                        a.nav__link(href="contacts.html") Contact


    script(src='https://code.jquery.com/jquery-1.12.4.min.js')
    script(src='https://code.jquery.com/jquery-migrate-1.2.1.min.js')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/fancybox/2.1.7/js/jquery.fancybox.min.js')
    script(src='assets/scripts/bundle.js' defer)
        